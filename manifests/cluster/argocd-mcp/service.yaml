apiVersion: v1
kind: Service
metadata:
  name: argocd-mcp
  namespace: argocd-mcp
spec:
  selector:
    app: argocd-mcp
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
