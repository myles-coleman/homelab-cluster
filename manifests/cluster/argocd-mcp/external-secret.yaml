apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: argocd-mcp-credentials
  namespace: argocd-mcp
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: bitwarden-notes
  target:
    name: argocd-mcp-credentials
    creationPolicy: Owner
  data:
  - secretKey: api-token
    remoteRef:
      key: 2465dd21-b3d2-499b-949e-e5619af1332f
