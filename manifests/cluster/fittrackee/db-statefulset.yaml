apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: fittrackee-db
  namespace: fittrackee
spec:
  serviceName: fittrackee-db-hl
  replicas: 1
  selector:
    matchLabels:
      app: fittrackee-db
  template:
    metadata:
      labels:
        app: fittrackee-db
    spec:
      containers:
        - name: postgres
          image: imresamu/postgis-arm64:17-recent-postgis3.5-geos3.13-proj9.5-gdal3.10-cgal6.0-sfcgal2.0-bookworm-ver20241107-1624f77-2024w48
          ports:
            - name: postgres
              containerPort: 5432
          env:
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: fittrackee-secrets
                  key: POSTGRES_DB
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: fittrackee-secrets
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: fittrackee-secrets
                  key: POSTGRES_PASSWORD
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - apiVersion: v1
      kind: PersistentVolumeClaim
      metadata:
        name: data
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: 8Gi
        storageClassName: longhorn

